name: CI

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13"]

    steps:
      - uses: actions/checkout@v7

      # Per uv docs, set up Python with the official action, then install uv.
      # https://docs.astral.sh/uv/guides/integration/github/
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v8.0.0
        with:
          enable-cache: true

      - name: Install project and dev dependencies (from lockfile)
        run: uv sync --extra dev --frozen

      - name: Check formatting (ruff format)
        run: uv run --frozen ruff format --check src tests

      - name: Lint (ruff check)
        run: uv run --frozen ruff check src tests

      - name: Run tests
        run: uv run --frozen pytest --cov=ocs_submission --cov-report=term-missing

      - name: Type check (mypy)
        run: uv run --frozen mypy src

      - name: Build package
        run: uv build

      - name: Check CLI entry point
        run: uv run --frozen ocs-submission --help
